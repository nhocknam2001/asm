{% extends 'base.html.twig' %}

{% block body %}
	<div class="container col-md-6 mt-3">
		<h3 class="text-success text-center">Room LIST</h3>
		<a class="btn btn-outline-success mt-2" href="{{ path('room_add') }}">ADD Room</a>
		<table class="table table-striped text-center mt-2">
			<thead>
				<tr>
					<th>ID</th>
					<th>Name</th>
					<th>Menu</th>
				</tr>
			</thead>
			<tbody>
				{% for room in rooms %}
					<tr>
						<td>{{ room.id }}</td>
						<td>
							<a class="text-info" href="{{ path('room_detail', {'id': room.id}) }}">
								{{ room.name }}
							</a>
						</td>
						<td>
                        {% if is_granted('ROLE_ADMIN') %}
							<a class="btn btn-info" href="{{ path('room_edit', {'id': room.id}) }}">EDIT</a>
							<a class="btn btn-warning" href="{{ path('room_delete', {'id': room.id}) }}" onclick="return confirm('Are you sure to delete this course ?')">DELETE</a>
                        {% endif %}
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}